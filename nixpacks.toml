# Railway Nixpacks config — needed for better-sqlite3 native compilation
[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake", "pkg-config"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:railway"]

[start]
cmd = "node dist/server.cjs"
